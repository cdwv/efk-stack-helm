apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-kibana-logging
spec:
  rules:
  - host: {{ .Values.ingress.host }}
    http:`
      paths:
      - path: /
        backend:
          serviceName: {{ .Release.Name }}-kibana-logging
          servicePort: 5601